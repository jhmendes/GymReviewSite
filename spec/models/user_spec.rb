# require 'rails_helper'
#
# RSpec.describe User, type: :model do
#
#   user = FactoryGirl.create(:user)
#   binding.pry
#   # it { should have_many(:gyms) }
#   # it { should have_many(:reviews) }
  # keep up with these 2 tests here, do I need to do the others? or does devise make sure things are valid????
#
#   it "is valid with valid attributes" do
#     # user = User.new(first_name: "John", last_name: "Mendes", email: "example@gmail.com")
#     expect(user).to be_valid
#   end
#
#   it "is not valid without a first_name" do
#     user = User.new(first_name: nil)
#     expect(user).to_not be_valid
#   end
#   it "is not valid without a last_name"
#   it "is not valid without a email"
#
# end

# email stays it wont let me re-run the tests
